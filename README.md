# Gizlock
Gizlock — Serverless encrypted publishing. Write a page, share a link. No accounts, no database, no server. Content is AES-256 encrypted and stored entirely inside the URL. Password protection, time-locked access, and NTP-verified expiry. 100% static HTML/CSS/JS.
